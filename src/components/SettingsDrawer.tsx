import { useEffect, useRef } from 'react';
import type { TimeZonePreference } from '../domain/types';
import { friendlyName } from '../platform/timezones';
import { TimezoneSelector } from './TimezoneSelector';
export function SettingsDrawer({ open, preference, query, onQuery, onAutomatic, onAlwaysOnChange, onStandardTimeChange, onClose }: { open: boolean; preference: TimeZonePreference; query: string; onQuery: (v: string) => void; onAutomatic: () => void; onAlwaysOnChange: (enabled: boolean) => void; onStandardTimeChange: (enabled: boolean) => void; onClose: () => void }) { const closeRef = useRef<HTMLButtonElement>(null); useEffect(() => { if (open) closeRef.current?.focus(); }, [open]); useEffect(() => { const handler = (event: KeyboardEvent) => { if (open && event.key === 'Escape') onClose(); }; document.addEventListener('keydown', handler); return () => document.removeEventListener('keydown', handler); }, [open, onClose]); if (!open) return null; return <aside className="drawer" aria-label="Clock settings"><div className="drawer-head"><h2>Settings</h2><button ref={closeRef} type="button" aria-label="Close settings" onClick={onClose}>×</button></div><button type="button" className={preference.mode === 'automatic' ? 'selected mode' : 'mode'} aria-pressed={preference.mode === 'automatic'} onClick={onAutomatic}>Automatic device timezone</button><TimezoneSelector query={query} onQuery={onQuery}/><label className="always-on-setting"><span>Show Standard Time</span><input type="checkbox" role="switch" checked={preference.showStandardTime} onChange={(event) => onStandardTimeChange(event.target.checked)}/></label><label className="always-on-setting"><span>Always On Mode</span><input type="checkbox" role="switch" checked={preference.alwaysOn} onChange={(event) => onAlwaysOnChange(event.target.checked)}/></label>{preference.mode === 'named' && <p className="current">Selected: {friendlyName(preference.timeZone)}</p>}</aside>; }