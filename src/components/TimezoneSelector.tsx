export function TimezoneSelector({ query, onQuery }: { query: string; onQuery: (value: string) => void }) { return <div className="timezone-selector"><label htmlFor="timezone-search">Search timezones</label><input id="timezone-search" value={query} onChange={(event) => onQuery(event.target.value)} placeholder="London, Tokyo..." autoComplete="off"/></div>; }