import { render, screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';
import { SettingsDrawer } from '../../src/components/SettingsDrawer';
describe('SettingsDrawer', () => { it('shows an Always On toggle below the timezone search without a timezone list', () => { render(<SettingsDrawer open preference={{ mode: 'automatic', alwaysOn: false }} query="" onQuery={() => undefined} onAutomatic={() => undefined} onAlwaysOnChange={() => undefined} onClose={() => undefined}/>); const search = screen.getByLabelText('Search timezones'); const toggle = screen.getByRole('switch', { name: 'Always On Mode' }); expect(screen.getByRole('complementary')).toBeInTheDocument(); expect(toggle).not.toBeChecked(); expect(search.compareDocumentPosition(toggle) & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy(); expect(screen.queryByRole('listbox', { name: 'Named timezones' })).not.toBeInTheDocument(); }); });