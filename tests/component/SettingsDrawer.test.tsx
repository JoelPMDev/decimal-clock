import { render, screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';
import { SettingsDrawer } from '../../src/components/SettingsDrawer';
describe('SettingsDrawer', () => { it('groups timezone, decimal view, and preference settings', () => { render(<SettingsDrawer open preference={{ mode: 'automatic', alwaysOn: false, showStandardTime: true, separator: ':' }} query="" onQuery={() => undefined} onAutomatic={() => undefined} onAlwaysOnChange={() => undefined} onStandardTimeChange={() => undefined} onSeparatorChange={() => undefined} onClose={() => undefined}/>); const search = screen.getByLabelText('Search timezones'); const separatorGroup = screen.getByRole('radiogroup', { name: 'Decimal separator' }); const separator = screen.getByRole('radio', { name: 'Colon (:)' }); const standardTimeToggle = screen.getByRole('switch', { name: 'Show Standard Time' }); const alwaysOnToggle = screen.getByRole('switch', { name: 'Always On Mode' }); expect(screen.getByRole('complementary')).toBeInTheDocument(); expect(screen.getByRole('heading', { name: 'Timezone' })).toBeInTheDocument(); expect(screen.getByRole('heading', { name: 'Decimal Time View' })).toBeInTheDocument(); expect(screen.getByRole('heading', { name: 'Preferences' })).toBeInTheDocument(); expect(separatorGroup).toBeInTheDocument(); expect(separator).toBeChecked(); expect(standardTimeToggle).toBeChecked(); expect(alwaysOnToggle).not.toBeChecked(); expect(search.compareDocumentPosition(separatorGroup) & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy(); expect(separatorGroup.compareDocumentPosition(standardTimeToggle) & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy(); expect(standardTimeToggle.compareDocumentPosition(alwaysOnToggle) & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy(); }); });