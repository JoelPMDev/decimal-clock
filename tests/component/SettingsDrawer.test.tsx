import { render, screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';
import { SettingsDrawer } from '../../src/components/SettingsDrawer';
describe('SettingsDrawer', () => { it('shows toggles for standard time and Always On below the timezone search', () => { render(<SettingsDrawer open preference={{ mode: 'automatic', alwaysOn: false, showStandardTime: true }} query="" onQuery={() => undefined} onAutomatic={() => undefined} onAlwaysOnChange={() => undefined} onStandardTimeChange={() => undefined} onClose={() => undefined}/>); const search = screen.getByLabelText('Search timezones'); const standardTimeToggle = screen.getByRole('switch', { name: 'Show Standard Time' }); const alwaysOnToggle = screen.getByRole('switch', { name: 'Always On Mode' }); expect(screen.getByRole('complementary')).toBeInTheDocument(); expect(standardTimeToggle).toBeChecked(); expect(alwaysOnToggle).not.toBeChecked(); expect(search.compareDocumentPosition(standardTimeToggle) & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy(); expect(standardTimeToggle.compareDocumentPosition(alwaysOnToggle) & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy(); expect(screen.queryByRole('listbox', { name: 'Named timezones' })).not.toBeInTheDocument(); }); });