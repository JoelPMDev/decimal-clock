import { beforeEach, describe, expect, it } from 'vitest';
import { loadPreference, PREFERENCE_KEY, savePreference } from '../../src/platform/persistence';
describe('preference persistence', () => { beforeEach(() => localStorage.clear()); it('round trips named records including standard time visibility', () => { expect(savePreference({ mode: 'named', timeZone: 'Europe/London', alwaysOn: true, showStandardTime: false })).toBe(true); expect(loadPreference()).toEqual({ mode: 'named', timeZone: 'Europe/London', alwaysOn: true, showStandardTime: false }); }); it('defaults legacy records to standard time visible and Always On disabled', () => { localStorage.setItem(PREFERENCE_KEY, JSON.stringify({ version: 1, mode: 'automatic' })); expect(loadPreference()).toEqual({ mode: 'automatic', alwaysOn: false, showStandardTime: true }); }); it('falls back for malformed records', () => { localStorage.setItem(PREFERENCE_KEY, '{bad'); expect(loadPreference()).toEqual({ mode: 'automatic', alwaysOn: false, showStandardTime: true }); }); });